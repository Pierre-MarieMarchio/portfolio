/**
 * What differs between deployments. Nothing here is secret: it ships in the
 * browser bundle like the rest.
 */
export const environment = {
  /** Appended to every page title, so a tab says whose site it is. */
  SITE_NAME: 'Pierre-Marie Marchio',
};
